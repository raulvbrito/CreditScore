platform :ios, '14.0'

target 'CreditScore' do
  use_frameworks!

  # Pods for CreditScore
  pod 'ReachabilitySwift' # Network state observer
  pod 'SnapKit' # Auto Layout DSL to facilitate View Code implementation
  pod 'SwiftLint' # Code analysis tool that keeps code cleaner by enforcing conventions

  target 'CreditScoreTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CreditScoreUITests' do
    # Pods for testing
  end

end
